The signature uppercase cyan kicker that labels every section.

```jsx
<Eyebrow>Clinic & Lab Services</Eyebrow>
<Eyebrow centered>Accredited HMO Partners</Eyebrow>
```

`centered` adds a trailing rule for centered headers. Pair above a `SectionHeading` or `<h2>`.

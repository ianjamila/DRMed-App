# Codex review of the pre-go-live fix batch (2026-09-11)

Model `gpt-6-astra`, reasoning xhigh, read-only, run against `origin/main...HEAD` of `fix/go-live-batch`.
Framed as a QA correctness review. 15 findings, 7 blockers. Verdict: hold the commit.

Orchestrator spot-checks before acting: findings 1, 3, 4 and 5 re-verified against the code by hand — all real.

I would hold this commit. I reviewed `origin/main...HEAD`; the findings below include new regressions and incomplete fixes where a sibling workflow was missed.

1. **P1 — Withdrawn chemistry results remain downloadable through other patient paths. Confidence: high.**  
   [Portal actions:128](/Users/jamila/Claude/DRMed/.worktrees/go-live-fixes/src/app/(patient)/portal/(authenticated)/actions.ts:128) checks every linked test only for the consolidated download action. [Individual downloads:218](/Users/jamila/Claude/DRMed/.worktrees/go-live-fixes/src/app/(patient)/portal/(authenticated)/actions.ts:218) and [ZIP exports:198](/Users/jamila/Claude/DRMed/.worktrees/go-live-fixes/src/app/(patient)/portal/(authenticated)/data-export/route.ts:198) still accept one released test. Release FBS and creatinine into one PDF, then undo FBS: downloading creatinine from the visit page—or exporting patient data—still delivers the PDF containing withdrawn FBS. **N6 is not delivered across all download paths.** These callers need one shared eligibility check.

2. **P1 — Retrying chemistry finalisation can publish a value the medtech cleared. Confidence: high.**  
   [finalise-consolidated.ts:315](/Users/jamila/Claude/DRMed/.worktrees/go-live-fixes/src/lib/actions/results/finalise-consolidated.ts:315) upserts submitted values without removing values omitted on retry. The form omits empty fields. After an upload failure, clearing an incorrect result and retrying leaves its previous database value intact; the PDF loader reads and prints it. Critical alerts are rebuilt from the new submission, so the retained value can also lose its alert. I reproduced the retained-value behavior with fault injection against the action. Replace the complete value set when resuming.

3. **P1 — A failed PDF metadata write still permits release and returns success. Confidence: high.**  
   [finalise-consolidated.ts:427](/Users/jamila/Claude/DRMed/.worktrees/go-live-fixes/src/lib/actions/results/finalise-consolidated.ts:427) ignores the result of updating `storage_path` and proceeds to release. An upload can succeed while that database write fails, leaving released tests whose patient download says no file exists. Fault injection reproduced `{ ok: true }`, status `released`, and `storage_path: null`. Check the update result before advancing. **N5’s downloadable-file guarantee is false.**

4. **P1 — An upload failure makes the normal chemistry retry route disappear. Confidence: high.**  
   [finalise-consolidated.ts:263](/Users/jamila/Claude/DRMed/.worktrees/go-live-fixes/src/lib/actions/results/finalise-consolidated.ts:263) sets `finalised_at` before generating the PDF. The junction trigger consequently advances ordinary chemistry tests to `ready_for_release`. But [the consolidated page:41](/Users/jamila/Claude/DRMed/.worktrees/go-live-fixes/src/app/(staff)/staff/(dashboard)/queue/consolidated/[visitId]/[groupId]/page.tsx:41) excludes that status. After an upload failure, refreshing or returning to the report redirects away instead of allowing retry. The visit’s release action also accepts that fileless ready state. Keep the result draft until its PDF is stored, and make recovery reachable.

5. **P1 — The consolidated entry page still exposes another section’s orders. Confidence: high.**  
   [Consolidated page:18](/Users/jamila/Claude/DRMed/.worktrees/go-live-fixes/src/app/(staff)/staff/(dashboard)/queue/consolidated/[visitId]/[groupId]/page.tsx:18) requires active staff but never checks `sectionsForRole`. Reception or an imaging technician opening a chemistry URL receives the patient’s name, ordered service codes and laboratory parameter names. Existing RLS permits these reads. The new gate on `/queue/[id]` does not protect this sibling route; its write actions denying access does not prevent the disclosure.

6. **P1 — Enabling gift-code payments exposes a double-redemption race. Confidence: high.**  
   [0139:49](/Users/jamila/Claude/DRMed/.worktrees/go-live-fixes/supabase/migrations/0139_gift_code_payment_method.sql:49) enables the previously failing payment insert, but [redemption:167](/Users/jamila/Claude/DRMed/.worktrees/go-live-fixes/src/app/(staff)/staff/(dashboard)/payments/new/actions.ts:167) assumes its conditional voucher update succeeded whenever there is no error. Two counters can both read `purchased` and insert payments. The second voucher update matches zero rows without error, leaving both payments credited. The new sale-adjustment uniqueness constraint does not cover redemption. Consume the voucher and create its payment in one transaction.

7. **P1 — Migration 0140 can fail against closed accounting periods. Confidence: high, conditional on existing data.**  
   [0140:473](/Users/jamila/Claude/DRMed/.worktrees/go-live-fixes/supabase/migrations/0140_manila_posting_dates.sql:473) updates posted journal entries without considering period locks. An affected pre-08:00 entry whose corrected Manila date falls in a closed period triggers [the existing `P0002` guard:47](/Users/jamila/Claude/DRMed/.worktrees/go-live-fixes/supabase/migrations/0029_gl_foundation_fixes.sql:47), aborting the migration. Empty replay cannot exercise this. The comment claiming the backfill “never raises” is incorrect; populated-data handling needs an explicit closed-period strategy.

8. **P2 — Voiding a gift-code cash sale leaves the voucher spendable. Confidence: high.**  
   New gift-sale adjustments appear in [the drawer’s generic Void control:191](/Users/jamila/Claude/DRMed/.worktrees/go-live-fixes/src/app/(staff)/staff/(dashboard)/payments/cash-drawer/cash-drawer-client.tsx:191). [Its action:133](/Users/jamila/Claude/DRMed/.worktrees/go-live-fixes/src/app/(staff)/staff/(dashboard)/payments/cash-drawer/actions.ts:133) reverses the adjustment without checking `kind` or updating `gift_codes`. Reception can refund/void a sale while its code remains `purchased` and redeemable. The new gift-code cancellation action handles the opposite direction, but this caller bypasses it. Gift sales need coordinated cancellation rather than generic adjustment voiding.

9. **P2 — Starting one visit can complete unrelated appointments. Confidence: high.**  
   [appointments/actions.ts:225](/Users/jamila/Claude/DRMed/.worktrees/go-live-fixes/src/app/(staff)/staff/(dashboard)/appointments/actions.ts:225) completes every arrived appointment for the patient, without matching the new visit’s services or intended booking. If someone has arrived for both laboratory work and a separate doctor consultation, creating only the laboratory visit from the patient page marks both completed. The consultation disappears despite remaining unserved. Unlike the adjacent appointment-specific helper, this exported action also never verifies that `visitId` belongs to the patient.

10. **P2 — The release-date backfill rewrites earlier release history using the latest release. Confidence: high.**  
    [0140:480](/Users/jamila/Claude/DRMed/.worktrees/go-live-fixes/supabase/migrations/0140_manila_posting_dates.sql:480) matches every journal entry sharing a test-request ID, including reversed entries. Release on July 1, undo on July 2, then re-release on August 1: the migration moves the original July journal entry to August 1 because that is the test’s current `released_at`. Its reversal retains July 2. This changes historical event dates by a month, rather than correcting a timezone offset. Restrict correction to entries whose original event timestamp can be established.

11. **P2 — Gift-code liability accounting does not cover supported redemption and sale behavior. Confidence: high.**  
    [0139:70](/Users/jamila/Claude/DRMed/.worktrees/go-live-fixes/supabase/migrations/0139_gift_code_payment_method.sql:70) debits account 2250 by the payment amount, while [redemption:141](/Users/jamila/Claude/DRMed/.worktrees/go-live-fixes/src/app/(staff)/staff/(dashboard)/payments/new/actions.ts:141) consumes the entire voucher and forfeits excess value. A ₱1,000 cash voucher used against ₱600 leaves ₱400 permanently recorded as outstanding. Separately, noncash sales create no sale journal entry, but their redemptions still debit 2250. The migration’s “nets to zero” claim holds only for cash vouchers redeemed at full face value.

12. **P2 — Forward postings use Manila dates, but payment voids and release reversals still use UTC dates. Confidence: high.**  
    Migration 0140 leaves [payment void posting:305](/Users/jamila/Claude/DRMed/.worktrees/go-live-fixes/supabase/migrations/0030_op_gl_bridge.sql:305) and [undo-release posting:35](/Users/jamila/Claude/DRMed/.worktrees/go-live-fixes/supabase/migrations/0110_undo_release.sql:35) unchanged. At 06:00 Manila on September 1, a payment/release now posts September 1; undoing it ten minutes later posts August 31. If August is closed, the admin’s reversal fails despite September being open. The date correction needs to cover these corresponding reversal paths.

13. **P2 — Older waiting walk-ins still disappear once there are 100 open service rows. Confidence: high.**  
    [appointments/page.tsx:183](/Users/jamila/Claude/DRMed/.worktrees/go-live-fixes/src/app/(staff)/staff/(dashboard)/appointments/page.tsx:183) replaces the date restriction with a newest-first limit of 100, without pagination or an overflow indication. These are appointment rows, not grouped bookings: roughly 34 three-service bookings can exceed it. The oldest waiting patients disappear; a booking spanning the boundary can also render only some of its services. **A9/N12’s visibility fix remains incomplete.** Page complete booking groups or provide access to the remainder.

14. **P2 — Operations screens and their newly paged exports calculate over different datasets. Confidence: high.**  
    [Daily CSV:37](/Users/jamila/Claude/DRMed/.worktrees/go-live-fixes/src/app/api/admin/operations/daily.csv/route.ts:37) now pages, but [the corresponding screen:41](/Users/jamila/Claude/DRMed/.worktrees/go-live-fixes/src/app/(staff)/staff/(dashboard)/admin/operations/page.tsx:41) still makes capped plain selects. A year containing over 1,000 day/section/channel rows gives truncated channel figures onscreen and fuller figures in the CSV. The screen’s independently fetched totals can also disagree with its channel breakdown. Cash, HMO and expense screens retain equivalent unpaged loaders. The broad claim that report figures stop silently truncating is not fulfilled.

15. **P2 — Email export paging stops at the patient-enrichment query. Confidence: high.**  
    [emails-log/query.ts:220](/Users/jamila/Claude/DRMed/.worktrees/go-live-fixes/src/lib/emails-log/query.ts:220) passes up to 10,000 audit rows into [an unpaged patient lookup:116](/Users/jamila/Claude/DRMed/.worktrees/go-live-fixes/src/lib/emails-log/query.ts:116). With over 1,000 distinct patients, that query truncates; its large UUID list can also exceed request limits. Errors are discarded. The export then silently contains missing recipient names and DRM-IDs while reporting the main export as complete. Chunk and validate this lookup too.

The ordinary registration → visit → conventional payment → receipt path remains connected. The receipt deletion predicate is shared correctly, and the new view/print auditing is wired through both receipt variants. The normal walk-in attachment path also completes, subject to findings 9 and 13. Chemistry finalisation, withdrawal/download and gift-code workflows have the blockers above.

For the migration replacements, 0137 restores imaging-role access while preserving the existing write conditions; 0138 preserves the package lifecycle checks while adding the HMO condition; 0139 preserves drawer denomination handling and includes salary payouts. Their empty-table operations are safe. I found no dropped policy or trigger left unrestored. The populated-data problems are in 0140’s backfills.

Validation: **108 test files / 1,215 tests passed; TypeScript passed; lint had zero errors and two warnings.** The fault-injection checks used mocked infrastructure around the actual action. I did not execute populated-database migrations or live browser workflows. No repository files were changed.